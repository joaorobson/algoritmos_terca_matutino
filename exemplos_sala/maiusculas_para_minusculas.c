#include <stdio.h>

int main(){
    char letra = 'D'; 

    printf("%c\n", letra);
    printf("%d\n", letra);
    // transformar de uma letra maiúscula para sua versão minúscula
    printf("%c\n", letra + 32);

    return 0;
}